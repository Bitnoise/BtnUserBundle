{% extends "::base_full.html.twig" %}

{% block body %}
    <div id="message">
        <h2>{{ 'resetting.check_email'|trans({'%email%': email}, 'FOSUserBundle') }}</h2>
        <a href="{{ path('magazine_all') }}" class="btn btn_no_icon btn_big"><span>Go to Homepage</span></a>
    </div>
{% endblock %}
